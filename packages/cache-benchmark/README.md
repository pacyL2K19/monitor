# cache-benchmark

Replay harness for benchmarking semantic cache implementations (BetterDB, RedisVL, GPTCache) against labeled query-pair datasets.

## Setup

```bash
uv sync
```

## Quick run

```bash
uv run cache-bench --help
```

## License

Apache 2.0
